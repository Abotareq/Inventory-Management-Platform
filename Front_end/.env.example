# Base URL the client calls. Leave unset in development to use the Vite proxy.
# VITE_API_BASE_URL=https://api.example.com/api

# Where the Vite dev proxy forwards /api (development only).
# VITE_API_PROXY_TARGET=http://localhost:5116
